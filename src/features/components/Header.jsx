import { Bell, Moon, RefreshCcw, User } from "lucide-react";
import React from "react";

const Header = () => {
  return (
    <header className="flex justify-between px-5 py-4 bg-black border border-black">
      <h1 className="font-heading font-bold text-white text-xl">Velix Bakery</h1>
      <div className="flex gap-5">
      </div>
    </header>
  );
};

export default Header;
